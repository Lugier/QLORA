# Core project dependencies.
# Torch + Unsloth are installed in scripts/runpod_setup.sh with explicit CUDA/Torch compatibility.

numpy<2
datasets==2.21.0
evaluate==0.4.3
vllm==0.6.6

pandas==2.2.2
pynvml==11.5.3

# Explicit training stack (avoid relying on transitive installs).
transformers>=4.45,<4.49
trl>=0.12,<0.16
peft>=0.12,<0.15
accelerate>=0.33,<1.0
huggingface_hub>=0.24,<1.0
